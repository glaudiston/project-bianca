<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView
        android:id="@+id/info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <RelativeLayout
        android:id="@+id/rect"
        android:layout_width="match_parent"
        android:layout_below="@+id/info"
        android:layout_height="200dp">
    </RelativeLayout>



    <Button
        android:id="@+id/btnDedos"
        android:layout_below="@+id/rect"
        android:text="Abrir ou Fechar a mão"
        android:layout_width="match_parent"
        android:enabled="false"
        android:layout_marginTop="15dp"
        android:layout_height="70dp" />

    <Button
        android:id="@+id/btnPunho"
        android:layout_below="@+id/btnDedos"
        android:text="Controlar Punho"
        android:layout_width="match_parent"
        android:enabled="false"
        android:layout_marginTop="5dp"
        android:layout_height="70dp" />

    <Button
        android:id="@+id/btnCotovelo"
        android:layout_below="@+id/btnPunho"
        android:text="Controlar Cotovelo"
        android:layout_width="match_parent"
        android:layout_marginTop="5dp"
        android:layout_height="70dp" />

    <Button
        android:id="@+id/btnOmbro"
        android:layout_below="@+id/btnCotovelo"
        android:text="Controlar Ombro"
        android:layout_width="match_parent"
        android:layout_marginTop="5dp"
        android:layout_height="70dp" />

</RelativeLayout>
